import { gridEntry } from './grid'

export class DiscordGrid {
  static join: gridEntry = {
    score: 3,
    isKeyAction: false,
  }

  static message: gridEntry = {
    score: 6,
    isKeyAction: true,
  }
}
