# Data Source created from Pipe 'projects_aggregation_mv'

SCHEMA >
    `id` String,
    `name` String,
    `slug` String,
    `description` String,
    `logo` String,
    `collectionSlug` String,
    `contributorCount` UInt64,
    `organizationCount` UInt64,
    `repositories` Array(Map(String, String))

ENGINE "ReplacingMergeTree"
ENGINE_SORTING_KEY "collectionSlug, slug"
