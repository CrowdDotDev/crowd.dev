SCHEMA >
    `memberId` String,
    `memberDisplayName` String,
    `memberAvatar` String,
    `memberRoles` Array(String),
    `organizationId` String,
    `organizationDisplayName` String,
    `organizationLogo` String,
    `activityCount` UInt64

ENGINE MergeTree
ENGINE_SORTING_KEY memberId, organizationId
