DESCRIPTION >
    Returns a list of activity types for a given segmentId, optionally filtered by repositories.

NODE activity_types
SQL >
    %
    SELECT DISTINCT a.type, a.platform, segmentId
    FROM activityRelations_deduplicated_cleaned_ds a
    WHERE
        segmentId = '{ segmentId }'
        {% if defined(repos) %}
            AND channel
            IN {{ Array(repos, 'String', description="Filter activity repo list", required=False) }}
        {% end %}
