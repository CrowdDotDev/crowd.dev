export enum OpensearchFieldType {
  UUID = 'uuid',
  UUID_ARR = 'uuid_arr',
  STRING = 'string',
  STRING_ARR = 'string_arr',
  INT = 'int',
  DATE = 'date',
  OBJECT = 'obj',
  OBJECT_ARR = 'obj_arr',
  FLOAT = 'float',
}

export enum OpenSearchIndex {
  MEMBERS = 'members',
}
