import noOfActivities from './noOfActivities'
import noOfMembers from './noOfMembers'
import activeOn from './activeOn'
import joinedDate from './joinedDate'
import lastActivityDate from './lastActivityDate'

export const memberFilters = {
  noOfActivities,
  noOfMembers,
  activeOn,
  joinedDate,
  lastActivityDate
}