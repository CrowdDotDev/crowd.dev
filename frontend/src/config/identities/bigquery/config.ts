import { IdentityConfig } from '@/config/identities';

const bigquery: IdentityConfig = {
  key: 'bigquery',
  name: 'BigQuery',
  image: '/images/identities/bigquery.svg',
  showInMembers: false,
  showInOrganizations: false,
};

export default bigquery;
