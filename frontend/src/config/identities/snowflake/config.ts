import { IdentityConfig } from '@/config/identities';

const snowflake: IdentityConfig = {
  key: 'snowflake',
  name: 'Snowflake',
  image: '/images/identities/snowflake.svg',
  showInMembers: false,
  showInOrganizations: false,
};

export default snowflake;
