import { IdentityConfig } from '@/config/identities';

const segment: IdentityConfig = {
  key: 'segment',
  name: 'Segment',
  image: '/images/identities/segment.svg',
  showInMembers: false,
  showInOrganizations: false,
};

export default segment;
