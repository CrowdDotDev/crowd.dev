import { IdentityConfig } from '@/config/identities';

const census: IdentityConfig = {
  key: 'census',
  name: 'Census',
  image: '/images/identities/census.png',
  showInMembers: false,
  showInOrganizations: false,
};

export default census;
