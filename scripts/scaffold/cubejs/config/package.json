{
  "name": "cubejs-project-template",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cubejs-server",
    "format": "npx prettier --write ."
  },
  "template": "docker",
  "templateVersion": "0.28.19",
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
