include classpath("application.conf")

node-address {
  host = "*"
}

queues {
  "nodejs-worker.fifo" {
    fifo = true
    contentBasedDeduplication = true
  }
  "python-worker.fifo" {
    fifo = true
    contentBasedDeduplication = true
  }
}

aws {
  region = "eu-central-1"
  accountId = 000000000000
}