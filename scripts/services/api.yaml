version: '3.1'

x-env-args: &env-args
  DOCKER_BUILDKIT: 1
  NODE_ENV: docker
  SERVICE: api
  TENANT_MODE: multi
  KUBE_MODE: 1

services:
  api:
    build:
      context: ../backend
      dockerfile: Dockerfile-api-dev
    command: 'npm run start:api'
    environment:
      <<: *env-args
    networks:
      - crowd-bridge

  api-dev:
    build:
      context: ../backend
      dockerfile: Dockerfile-api-dev
    command: 'npm run start:api'
    environment:
      <<: *env-args
    hostname: api
    networks:
      - crowd-bridge
    volumes:
      - ../../backend:/usr/crowd/backend

networks:
  crowd-bridge:
    external: true
